<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.aliyun.hbase</groupId>
    <artifactId>hbase-root</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Aliyun Apsaradb Hbase Demo</name>

    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <organization>
        <name>Alibaba Cloud Computing</name>
        <url>http://www.aliyun.com</url>
    </organization>

    <properties>
        <scala.version>2.11.8</scala.version>
        <scala.binary.version>2.11</scala.binary.version>
        <spark.connectors.version>1.0.4</spark.connectors.version>
        <spark.version>2.4.3</spark.version>
        <maxcompute.version>0.28.4-public</maxcompute.version>
        <hbase.version>1.1.3</hbase.version>
        <solr.version>7.3.1</solr.version>
        <mongo.client.version>2.4.0</mongo.client.version>
        <kafka.client.version>0.10.2.2</kafka.client.version>
        <datahu.client.version>2.9.2-public</datahu.client.version>
        <loghubb.client.version>0.6.13</loghubb.client.version>
        <tablestore.sdk.version>4.11.0</tablestore.sdk.version>
        <oss.sdk.version>3.1.0</oss.sdk.version>
        <spark.connectors.mongo.version>${mongo.client.version}_${spark.version}-${spark.connectors.version}</spark.connectors.mongo.version>
        <spark.connectors.datahub.version>${datahu.client.version}_${spark.version}-${spark.connectors.version}</spark.connectors.datahub.version>
        <spark.connectors.hbase.version>${hbase.version}_${spark.version}-${spark.connectors.version}</spark.connectors.hbase.version>
        <spark.connectors.maxcompute.version>${maxcompute.version}_${spark.version}-${spark.connectors.version}</spark.connectors.maxcompute.version>
        <spark.connectors.loghub.version>${loghubb.client.version}_${spark.version}-${spark.connectors.version}</spark.connectors.loghub.version>
        <spark.connectors.tablestore.version>${tablestore.sdk.version}_${spark.version}-${spark.connectors.version}</spark.connectors.tablestore.version>
    </properties>

    <modules>
        <module>spark/spark-examples</module>
        <module>phoenix/phoenix-4.x</module>
        <module>spark/spark-jdbc</module>
        <module>spark/sparkstreaming</module>
        <module>spark/common-test</module>
        <module>hbase</module>
        <module>solr</module>
        <module>phoenix/phoenix-5.x</module>
        <module>spark/structuredstreaming</module>
        <module>phoenix/phoenix-mybatis</module>
    </modules>

</project>
